1. fast, scalable, fault-tolerant, publish-subscribe messaging system
2. highly available and resilient to node failures and supports automatic recovery
3. Components of Kafka: a) broker b) producer c) consumer
4. broker: is a node on kafka cluster which is used to persist and replicate the data. 
5. producer: pushes the message into the message container called kafka topic
6. consumer: pulls the message from the kafka topic


MESSAGING SYSTEM IN KAFKA

distributed messaging is based on reliable message queuing 
messages are asynchronously queued between client applications and messaging systems. 

there are 2 types of messaging patterns available;
a) point to point
b) publish-subscribe messaging system. 

